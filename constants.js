const IP = 'localhost';

const PORT = 50541;
const NAME = 'SAM';

const MOVE_UP = 'w';
const MOVE_DOWN = 's';
const MOVE_RIGHT = 'd';
const MOVE_LEFT = 'a';

const MESSAGES = {
  'x': 'I will win',
  'f': "I'am faster than you",
  'g': 'GoodBye'
};

module.exports = {IP, PORT, NAME, MOVE_UP, MOVE_DOWN, MOVE_RIGHT, MOVE_LEFT, MESSAGES };
